<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sideMenuTabItem="clr-namespace:ProMedScope.Controls.SideMenu.TabItem;assembly=ProMedScope.Controls"
                    xmlns:icon="clr-namespace:ProMedScope.Controls.Icon;assembly=ProMedScope.Controls">

    <!-- SideMenuTabItemStyle -->
    <Style x:Key="SideMenuTabItemStyle" TargetType="{x:Type sideMenuTabItem:PmSideMenuTabItem}">
        <Setter Property="Height" Value="70"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type sideMenuTabItem:PmSideMenuTabItem}">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}">

                            <!-- Icon -->
                            <icon:PmIcon x:Name="Icon" 
                                         Height="22" Width="22"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"
                                         IconMaterial="{TemplateBinding Icon}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>