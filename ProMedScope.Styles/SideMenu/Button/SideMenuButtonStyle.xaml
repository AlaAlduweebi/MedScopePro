<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:icon="clr-namespace:ProMedScope.Controls.Icon;assembly=ProMedScope.Controls"
                    xmlns:sideMenuButton="clr-namespace:ProMedScope.Controls.SideMenu._Elements.Button;assembly=ProMedScope.Controls">

    <!-- SideMenuButtonStyle -->
    <Style x:Key="SideMenuButtonStyle" TargetType="{x:Type sideMenuButton:PmSideMenuButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type sideMenuButton:PmSideMenuButton}">
                    <Border x:Name="Border"
					        Background="{TemplateBinding Background}"
					        BorderBrush="{TemplateBinding BorderBrush}"
					        BorderThickness="{TemplateBinding BorderThickness}">

                            <!-- Icon -->
                        <icon:PmIcon Grid.Column="0"
                                     x:Name="Icon"
						             Height="32" Width="32"
						             IconMaterial="{TemplateBinding Icon}"
						             Foreground="{TemplateBinding Foreground}"/>
                        
                    </Border>
                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="#FF3A36" />
                        </Trigger>
                        
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>