<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sideMenuItem="clr-namespace:ProMedScope.Controls.SideMenu._Elements.Item;assembly=ProMedScope.Controls"
                    xmlns:icon="clr-namespace:ProMedScope.Controls.Icon;assembly=ProMedScope.Controls">

    <!-- SideMenuItemStyle -->
    <Style x:Key="SideMenuItemStyle" TargetType="{x:Type sideMenuItem:PmSideMenuItem}">
        <Setter Property="Height" Value="70"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type sideMenuItem:PmSideMenuItem}">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}">
                        <Grid>

                            <!-- SelectedLine -->
                            <Border x:Name="SelectedLine"
                                    Height="46" Width="4"
                                    Background="{TemplateBinding Foreground}"
                                    HorizontalAlignment="Left"
                                    CornerRadius="2"
                                    Visibility="Collapsed"/>

                            <!-- Icon -->
                            <icon:PmIcon x:Name="Icon" 
                                         IconMaterial="{TemplateBinding Icon}"
                                         Foreground="{TemplateBinding Foreground}"
                                         Height="32" Width="32"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="#2978F5" />
                        </Trigger>
                        
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="SelectedLine" Property="Visibility" Value="Visible" />
                            <Setter Property="Foreground" Value="#2978F5" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>