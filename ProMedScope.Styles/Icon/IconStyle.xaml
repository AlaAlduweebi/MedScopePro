<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:icon="clr-namespace:ProMedScope.Controls.Icon;assembly=ProMedScope.Controls">

    <!-- IconStyle -->
    <Style x:Key="IconStyle" TargetType="{x:Type icon:PmIcon}">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type icon:PmIcon}">
                    <Border x:Name="Border" 
							Background="Transparent">

                        <!-- ImageIcon -->
                        <Image x:Name="ImageIcon"
						       Width="{TemplateBinding Width}"
						       Height="{TemplateBinding Height}"
                               Source="{TemplateBinding RenderedDrawingImage}" 
						       Stretch="Uniform"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>