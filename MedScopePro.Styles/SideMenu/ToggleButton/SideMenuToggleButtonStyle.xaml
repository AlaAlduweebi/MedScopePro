<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sideMenuToggleButton="clr-namespace:MedScopePro.Controls.SideMenu._Elements.ToggleButton;assembly=MedScopePro.Controls"
                    xmlns:icon="clr-namespace:MedScopePro.Controls.Icon;assembly=MedScopePro.Controls">

    <!-- SideMenuToggleButtonStyle -->
    <Style x:Key="SideMenuToggleButtonStyle" TargetType="{x:Type sideMenuToggleButton:SideMenuToggleButtonControl}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type sideMenuToggleButton:SideMenuToggleButtonControl}">
                    <Border x:Name="Border"
					        Background="{TemplateBinding Background}"
					        BorderBrush="{TemplateBinding BorderBrush}"
					        BorderThickness="{TemplateBinding BorderThickness}">

                            <!-- Icon -->
                        <icon:IconControl Grid.Column="0"
                                          x:Name="Icon"
						                  Height="32" Width="32"
						                  IconMaterial="{TemplateBinding Icon}"
						                  Foreground="{TemplateBinding Foreground}"/>
                        
                    </Border>
                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="#FF3A36" />
                        </Trigger>
                        
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>