<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sideMenuItem="clr-namespace:MedScopePro.Controls.SideMenu._Elements.Item;assembly=MedScopePro.Controls"
                    xmlns:icon="clr-namespace:MedScopePro.Controls.Icon;assembly=MedScopePro.Controls">

    <!-- SideMenuItemStyle -->
    <Style x:Key="SideMenuItemStyle" TargetType="{x:Type sideMenuItem:SideMenuItemControl}">
        <Setter Property="Height" Value="70"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type sideMenuItem:SideMenuItemControl}">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}">
                        <Grid>

                            <!-- SelectedLine -->
                            <Border x:Name="SelectedLine"
                                    Height="46" Width="4"
                                    Background="{TemplateBinding Foreground}"
                                    HorizontalAlignment="Left"
                                    CornerRadius="2"
                                    Visibility="Collapsed"/>

                            <!-- Icon -->
                            <icon:IconControl x:Name="Icon" 
                                              IconMaterial="{TemplateBinding Icon}"
                                              IconStyle="Fill"
                                              Foreground="{TemplateBinding Foreground}"
                                              Height="32" Width="32"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="#2978F5" />
                        </Trigger>
                        
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="SelectedLine" Property="Visibility" Value="Visible" />
                            <Setter Property="Foreground" Value="#2978F5" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>